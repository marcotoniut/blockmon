[profile.default]
src = "src"
test = "test"
out = "out"
libs = []
solc_version = "0.8.24"
optimizer = false

[fmt]
# 120 is pinned, not inherited: Solidity style guide width. If the
# formatter default shifted, the fmt gate would rewrite the contract.
line_length = 120
