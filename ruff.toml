# conformance/check.py is an independent G0a checker
# using stdlib only, so no rule may suggest third-party deps.
target-version = "py313"
line-length = 100

[lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM", "C4", "RUF"]
